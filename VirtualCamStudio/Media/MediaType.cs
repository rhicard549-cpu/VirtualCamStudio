namespace VirtualCamStudio.Media
{
    /// <summary>
    /// Represents the type of media file.
    /// </summary>
    public enum MediaType
    {
        /// <summary>
        /// Static image file (JPG, PNG, BMP, etc.)
        /// </summary>
        Image,

        /// <summary>
        /// Video file (MP4, MOV, AVI, MKV, etc.)
        /// </summary>
        Video
    }
}
